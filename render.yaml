services:
  - type: web
    name: tehrent
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
